# 管理员账号说明

**最后更新:** 2026-01-23

---

## 📋 默认管理员账号

### 登录信息

```
账号: admin
密码: admin
角色: 超级管理员
```

⚠️ **重要提示:** 首次登录后请立即修改密码!

---

## 🔧 管理员账号管理

### 1. 检查管理员账号

**脚本:** `scripts/check-and-fix-admin.js`

```bash
# 在服务器上运行
ssh root@47.104.173.139
cd /www/wwwroot/exammaster.zzzjl.com
docker cp scripts/check-and-fix-admin.js edumaster_api:/app/
docker exec edumaster_api node /app/check-and-fix-admin.js
```

**功能:**
- ✅ 检查数据库连接
- ✅ 检查所有数据表
- ✅ 检查管理员账号是否存在
- ✅ 如果不存在则自动创建
- ✅ 测试密码验证

### 2. 重置管理员密码

**脚本:** `scripts/reset-admin-password.js`

```bash
# 在服务器上运行
ssh root@47.104.173.139
cd /www/wwwroot/exammaster.zzzjl.com
docker cp scripts/reset-admin-password.js edumaster_api:/app/
docker exec edumaster_api node /app/reset-admin-password.js
```

**功能:**
- ✅ 将管理员密码重置为 `admin`
- ✅ 验证密码是否重置成功

### 3. 手动创建管理员账号

如果脚本无法运行,可以手动在数据库中创建:

```bash
# 连接到数据库
docker exec -it edumaster_postgres psql -U edumaster_user -d edumaster

# 创建管理员账号 (密码: admin)
INSERT INTO users (
  id, phone, password, role, real_name, nickname, permissions, created_at, updated_at
) VALUES (
  'admin-1',
  'admin',
  '$2a$10$YourHashedPasswordHere',  -- 需要使用 bcrypt 加密
  'ADMIN',
  '超级管理员',
  '管理员',
  '{"dashboard":true,"studentManagement":true,"bankManagement":true,"examPublish":true,"practicalPublish":true,"supervision":true,"discussionManagement":true,"tagManagement":true,"systemSettings":true}',
  CURRENT_TIMESTAMP,
  CURRENT_TIMESTAMP
);
```

---

## 🔐 修改密码

### 方法 1: 通过系统界面

1. 使用 `admin/admin` 登录系统
2. 点击右上角头像
3. 选择"修改密码"
4. 输入旧密码和新密码
5. 保存

### 方法 2: 通过数据库

```bash
# 连接到数据库
docker exec -it edumaster_postgres psql -U edumaster_user -d edumaster

# 更新密码 (需要先用 bcrypt 加密新密码)
UPDATE users 
SET password = '$2a$10$NewHashedPasswordHere' 
WHERE phone = 'admin' AND role = 'ADMIN';
```

### 方法 3: 使用 Node.js 脚本

```javascript
// 在容器中运行
docker exec edumaster_api node -e "
const bcrypt = require('bcryptjs');
const newPassword = 'your_new_password';
const hash = bcrypt.hashSync(newPassword, 10);
console.log('Hashed password:', hash);
"
```

---

## 🚨 常见问题

### 问题 1: 登录提示"账号或密码错误"

**原因:**
- 管理员账号不存在
- 密码不正确
- 数据库连接失败

**解决方法:**

```bash
# 1. 检查管理员账号
docker cp scripts/check-and-fix-admin.js edumaster_api:/app/
docker exec edumaster_api node /app/check-and-fix-admin.js

# 2. 如果账号存在但密码错误,重置密码
docker cp scripts/reset-admin-password.js edumaster_api:/app/
docker exec edumaster_api node /app/reset-admin-password.js

# 3. 如果账号不存在,脚本会自动创建
```

### 问题 2: 重置密码后仍然无法登录

**可能原因:**
- 浏览器缓存
- API 服务未重启
- 数据库连接问题

**解决方法:**

```bash
# 1. 清除浏览器缓存或使用无痕模式

# 2. 重启 API 服务
docker compose restart api

# 3. 检查 API 日志
docker logs edumaster_api --tail=50

# 4. 测试健康检查
curl http://localhost:3001/api/health
```

### 问题 3: 脚本无法运行

**错误信息:** `no such file or directory`

**解决方法:**

```bash
# 1. 确保脚本文件存在
ls -la scripts/reset-admin-password.js

# 2. 上传脚本到服务器
scp scripts/reset-admin-password.js root@47.104.173.139:/tmp/

# 3. 复制到容器
docker cp /tmp/reset-admin-password.js edumaster_api:/app/

# 4. 运行脚本
docker exec edumaster_api node /app/reset-admin-password.js
```

### 问题 4: 数据库中没有 users 表

**原因:** 数据库未初始化

**解决方法:**

```bash
# 1. 停止服务
docker compose down

# 2. 删除数据卷
docker volume rm edumaster_postgresql_postgres_data

# 3. 重新启动 (会自动初始化数据库)
docker compose up -d

# 4. 等待初始化完成
sleep 30

# 5. 创建管理员账号
docker cp scripts/check-and-fix-admin.js edumaster_api:/app/
docker exec edumaster_api node /app/check-and-fix-admin.js
```

---

## 📝 部署时自动处理

`deploy.bat` 脚本已更新,会在部署时自动重置管理员密码:

```batch
deploy.bat
```

**自动执行:**
1. ✅ 构建和打包
2. ✅ 上传到服务器
3. ✅ 部署服务
4. ✅ 重置管理员密码 (新增)
5. ✅ 验证部署结果

---

## 🔒 安全建议

1. **立即修改默认密码**
   - 首次登录后立即修改
   - 使用强密码 (至少 8 位,包含大小写字母、数字、特殊字符)

2. **定期更换密码**
   - 建议每 90 天更换一次
   - 不要重复使用旧密码

3. **限制管理员数量**
   - 只创建必要的管理员账号
   - 定期审查管理员列表

4. **启用操作日志**
   - 记录所有管理员操作
   - 定期审查日志

5. **使用 HTTPS**
   - 生产环境必须启用 SSL
   - 防止密码在传输中被窃取

---

## 📞 获取帮助

### 相关脚本

- `scripts/check-and-fix-admin.js` - 检查和创建管理员账号
- `scripts/reset-admin-password.js` - 重置管理员密码
- `deploy.bat` - 部署脚本 (包含自动重置密码)

### 相关文档

- `部署指南.md` - 部署说明
- `完整部署指南.md` - 详细文档
- `README.md` - 项目概述

### 常用命令

```bash
# 检查管理员账号
docker exec edumaster_api node /app/check-and-fix-admin.js

# 重置管理员密码
docker exec edumaster_api node /app/reset-admin-password.js

# 查看 API 日志
docker logs edumaster_api -f

# 重启 API 服务
docker compose restart api

# 测试健康检查
curl http://localhost:3001/api/health
```

---

**文档更新时间:** 2026-01-23  
**默认密码:** admin (请立即修改!)
