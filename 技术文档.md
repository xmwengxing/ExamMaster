# EduMaster 项目技术文档

## 项目概述

**项目名称**: EduMaster - 全栈智能刷题与模拟考试系统  
**版本**: 4.0.0 (模块化架构版)  
**更新日期**: 2026-01-26  
**域名**: https://exammaster.zzzjl.com  
**服务器**: 阿里云 ECS (47.104.173.139)  
**GitHub**: https://github.com/xmwengxing/EduMaster_postgresql

## 🎉 最新更新 (v4.0.0 - 2026-01-26)

**模块化重构完成！** 将 4100+ 行的单体 `server.js` 重构为清晰的模块化架构：
- ✅ **12 个业务模块**: auth, user, bank, question, practice, srs, exam, practical, discussion, ai, tag, admin, system
- ✅ **492 个测试用例**: 100% 通过率，测试覆盖率 ≥70%
- ✅ **三层架构设计**: 路由层 → 控制器层 → 服务层
- ✅ **完整的文档体系**: 规范文档、技术文档、API 文档
- ✅ **生产环境部署**: 已成功部署并验证
- ✅ **自动化部署**: 快速部署脚本，支持备份回滚

**模块化架构优势：**
- 📦 代码可维护性显著提升（4100行 → 50+文件）
- 🧪 测试覆盖率从 0% → ≥70%
- 🚀 部署时间从 ~10分钟 → <5分钟
- 🔄 向后兼容，无需前端修改

**详细文档：**
- [模块化重构项目总结](docs/模块化重构项目总结.md) - 完整的项目总结和统计
- [生产环境部署报告](docs/生产环境部署报告.md) - 部署流程和验证结果
- [模块依赖关系](docs/模块依赖关系.md) - 架构设计和模块说明
- [文档中心](docs/README.md) - 所有文档索引
- [技术文档索引](备忘文件/README.md) - 运维和开发文档

## 技术栈

### 前端技术
- **框架**: React 19 + TypeScript
- **构建工具**: Vite 6
- **UI 框架**: Tailwind CSS 3.4.1 (CDN)
- **图标库**: Font Awesome 6.4.2
- **状态管理**: Zustand 5.0.2
- **本地存储**: Dexie 4.0.11 (IndexedDB)
- **图表库**: Recharts 3.6.0
- **Markdown**: Marked 5.1.1
- **富文本编辑**: Quill 2.0.3

### 后端技术
- **运行环境**: Node.js 18+
- **框架**: Express 5
- **数据库**: PostgreSQL 14+ ✨ 已迁移
- **数据库驱动**: node-postgres (pg) ✨ 新增
- **认证**: JWT (jsonwebtoken 9.0.3)
- **密码加密**: bcryptjs 3.0.3
- **日志**: Winston ✨ 新增
- **AI 集成**: 
  - DeepSeek API (推荐)
  - OpenAI API
  - Claude API (Anthropic)
  - Gemini API (Google)
  - 文心一言 API (百度)
- **跨域**: CORS 2.8.5

### 部署技术
- **容器化**: Docker + Docker Compose ✨ 新增
- **Web 服务器**: Nginx (反向代理 + HTTPS)
- **操作系统**: CentOS 7.8
- **SSL 证书**: Let's Encrypt
- **CI/CD**: GitHub Actions ✨ 新增

## 系统架构

### 整体架构

**注意**: 以下是标准 Docker 部署架构。实际生产服务器使用宝塔面板管理 Nginx，详见 [服务器实际架构说明.md](服务器实际架构说明.md)。

```
┌─────────────────┐
│   用户浏览器     │
└────────┬────────┘
         │ HTTPS (443)
         ↓
┌─────────────────┐
│  Nginx          │ (端口 80/443)
│  反向代理 + SSL │
│  ⚠️ 生产环境:   │
│  宝塔面板管理   │
└────────┬────────┘
         │
         ├─→ 静态文件 (dist/)
         │
         └─→ API 请求 (proxy_pass)
              ↓
    ┌─────────────────┐
    │  API 容器       │ (端口 3001)
    │  Express + Node │
    └────────┬────────┘
             │
             ↓
    ┌─────────────────┐
    │ PostgreSQL 容器 │ (端口 5432)
    │  数据持久化     │
    └─────────────────┘
```

### Docker 容器架构
- **postgres**: PostgreSQL 14-alpine 数据库容器
- **api**: Node.js 18-alpine API 服务容器
- **nginx**: Nginx-alpine Web 服务器容器（⚠️ 生产环境未使用，由宝塔面板管理）

### 生产环境架构说明

**重要**: 生产服务器 (47.104.173.139) 的实际架构与标准 Docker 部署有所不同：

1. **Nginx 管理方式**
   - ❌ 不在 Docker 容器中
   - ✅ 由宝塔面板管理（宿主机）
   - 配置文件: `/www/server/nginx/conf/nginx.conf`
   - 项目 `nginx/` 目录的配置**未被使用**

2. **SSL 证书管理**
   - ❌ 不在项目 `nginx/ssl/` 目录
   - ✅ 由宝塔面板管理和自动续期
   - 证书路径: `/www/server/nginx/...`

3. **容器内文件**
   - API 容器只包含核心文件（server.js, db.js, package.json 等）
   - ❌ 容器中没有 `scripts/` 目录
   - ❌ 容器中没有 `dist/` 目录（前端由 Nginx 直接服务）

4. **部署命令**
   - 使用 `docker` 命令（新版 Docker Compose）
   - 不使用 `docker-compose` 命令

详细信息请参考: [服务器实际架构说明.md](服务器实际架构说明.md)

### 目录结构

```
/www/wwwroot/exammaster.zzzjl.com/
├── docker-compose.yml       # Docker 编排配置
├── .env                     # 环境变量配置
├── .env.example            # 环境变量模板
├── .env.production         # 生产环境配置
├── Dockerfile              # API 容器构建文件
├── server-new.js           # Express 后端服务（模块化版本）✨
├── server.js               # Express 后端服务（旧版本，保留参考）
├── db.js                   # PostgreSQL 连接池
├── package.json            # 项目依赖配置
├── dist/                   # 前端构建产物
│   ├── index.html
│   └── assets/
├── src/                    # 模块化后端代码 ✨ 新增
│   ├── config/            # 配置模块（cors, jwt, constants）
│   ├── utils/             # 工具函数（parsers, validators, helpers）
│   ├── middleware/        # 中间件（auth, errorHandler, requestLogger）
│   ├── services/          # 服务层（12个业务模块）
│   ├── controllers/       # 控制器层（12个业务模块）
│   └── routes/            # 路由层（12个业务模块 + 聚合器）
├── tests/                 # 测试代码 ✨ 新增
│   ├── unit/             # 单元测试（300+个）
│   └── integration/      # 集成测试（192+个）
├── utils/                 # 工具函数（logger, questionValidation）
├── nginx/                 # Nginx 配置（⚠️ 生产环境未使用）
│   ├── nginx.conf
│   └── ssl/              # SSL 证书
├── postgres/             # PostgreSQL 配置
│   ├── init.sql         # 数据库初始化脚本
│   ├── data/            # 数据持久化目录
│   ├── pg_hba.conf      # 访问控制配置
│   └── postgresql.conf  # 数据库配置
├── logs/                 # 应用日志
│   ├── app-YYYY-MM-DD.log
│   └── error-YYYY-MM-DD.log
├── backups/             # 数据库备份
├── scripts/             # 运维脚本
│   ├── sync-to-github.bat      # GitHub 同步脚本 ✨
│   ├── pack-and-upload.js      # 部署打包脚本
│   └── README.md
├── 服务器脚本/           # 服务器专用脚本
│   ├── backup.sh        # 数据库备份
│   ├── restore.sh       # 数据库恢复
│   ├── monitor-database.sh
│   ├── deploy.sh        # 自动部署
│   ├── rollback.sh      # 回滚脚本
│   └── cleanup-and-optimize.sh
├── docs/                # 技术文档 ✨
│   ├── README.md        # 文档中心索引
│   ├── 模块化重构项目总结.md
│   ├── 生产环境部署报告.md
│   └── 模块依赖关系.md
├── 备忘文件/            # 运维文档 ✨
│   └── README.md        # 技术文档索引
├── .kiro/              # 规范文档 ✨
│   └── specs/
│       └── server-modularization/
│           ├── requirements.md  # 需求文档
│           ├── design.md       # 设计文档
│           └── tasks.md        # 任务清单
├── deploy.bat          # 完整部署脚本 ✨
├── quick-deploy.bat    # 快速部署脚本 ✨
├── 题库转换脚本Python版.bat  # 题库转换工具 ✨
└── node_modules/       # Node.js 依赖包
```

**重要说明：**
- ✨ 标记为新增或重要文件
- ⚠️ `nginx/` 目录配置在生产环境未使用（由宝塔面板管理）
- 🔧 `server-new.js` 是模块化后的新入口文件
- 📦 `src/` 目录包含所有模块化后的业务代码
- 🧪 `tests/` 目录包含 492 个测试用例

## 核心功能模块

### 1. 用户管理模块
- **角色系统**: 
  - 超级管理员 (ADMIN - phone: 'admin')
  - 普通管理员 (ADMIN with permissions)
  - 学员 (STUDENT)
- **权限管理**: 基于角色的访问控制 (RBAC)
- **认证机制**: JWT Token (7天有效期)
- **密码加密**: bcrypt 加密存储 (10轮)
- **自定义字段**: 支持管理员自定义学员信息字段

### 2. 题库管理模块
- **题库分类**: 支持多级分类和难度等级
- **题型支持**: 单选、多选、判断、填空、简答
- **题目导入**: 批量导入功能，支持 CSV 格式
- **分值配置**: 每个题库可自定义各题型分值
- **题目标签**: 支持标签分类和管理
- **题目去重**: 自动检测重复题目

### 3. 练习模块
- **练习模式**: 顺序、随机、错题、收藏、智能复习(SRS)
- **进度保存**: 自动保存答题进度
- **答题记录**: 完整的答题历史记录
- **题型筛选**: 支持按题型筛选练习

### 4. 考试模块
- **考试策略**: 随机抽题、手动选题
- **考试配置**: 时长、及格分数、题目数量、可见性控制
- **考试历史**: 完整的考试记录和成绩统计
- **成绩分析**: 错题统计、用时分析

### 5. 实操题模块
- **多部分题目**: 支持复杂的实操题结构
- **AI 评价**: 集成多个 AI 服务商进行智能评价
- **提交记录**: 保存学员的实操答案和历史记录

### 6. AI 功能模块
- **多AI服务商支持**: DeepSeek、OpenAI、Claude、Gemini、文心一言
- **题目讲解**: AI 智能讲解题目
- **语音朗读**: TTS 文字转语音功能
- **实操评价**: AI 评价实操题答案
- **自动评分**: 简答题 AI 自动评分
- **API 配置**: 支持全局和个人 API Key

### 7. 讨论社区模块
- **讨论发起**: 学员可发起讨论，支持关联题目
- **评论系统**: 多级评论，支持回复
- **点赞功能**: 讨论和评论点赞
- **管理功能**: 置顶、隐藏、删除讨论
- **富文本编辑**: 支持富文本内容编辑

### 8. 标签管理模块
- **标签创建**: 创建题目标签，支持自定义颜色
- **标签编辑**: 修改标签名称和颜色
- **标签合并**: 合并重复标签
- **使用统计**: 显示标签使用次数

### 9. 数据统计模块
- **学员统计**: 答题数量、正确率、错题数量、每日进度、在线时长
- **管理员统计**: 题库使用情况、考试通过率、学员活跃度、登录日志
- **数据看板**: 可视化图表展示

### 10. 系统设置模块
- **主题定制**: 页面标题、Logo、登录页标语
- **内容管理**: 首页公告、轮播横幅、视频课程、首页布局
- **系统安全**: 数据维护、管理员密码更新、数据清理
- **智能化配置**: AI 模型选择、自动评分规则、TTS 语音设置

## 数据库设计 (PostgreSQL)

### 数据类型映射

| SQLite 类型 | PostgreSQL 类型 | 说明 |
|------------|----------------|------|
| TEXT | VARCHAR/TEXT | 文本字段 |
| INTEGER | INTEGER/BIGINT | 整数字段 |
| REAL | NUMERIC/DOUBLE PRECISION | 浮点数 |
| TEXT (JSON) | JSONB | JSON 数据，使用 JSONB 提升性能 |
| TEXT (日期) | TIMESTAMP | 日期时间字段 |

### 核心数据表

#### users (用户表)
```sql
CREATE TABLE users (
  id VARCHAR(255) PRIMARY KEY,
  phone VARCHAR(50) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL,
  role VARCHAR(20) NOT NULL,
  real_name VARCHAR(100),
  nickname VARCHAR(100),
  avatar TEXT,
  id_card VARCHAR(50),
  school VARCHAR(200),
  education_type VARCHAR(50),
  education_level VARCHAR(50),
  major VARCHAR(100),
  company VARCHAR(200),
  custom_fields JSONB,
  student_perms JSONB,
  allowed_bank_ids JSONB,
  accuracy NUMERIC(5,2),
  mistake_count INTEGER DEFAULT 0,
  daily_goal INTEGER DEFAULT 20,
  last_login TIMESTAMP,
  permissions JSONB,
  deepseek_api_key TEXT,
  login_history JSONB,
  total_online_time INTEGER DEFAULT 0,
  class_name VARCHAR(100),
  last_activity TIMESTAMP,
  gender VARCHAR(10),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_users_phone ON users(phone);
CREATE INDEX idx_users_role ON users(role);
CREATE INDEX idx_users_last_activity ON users(last_activity);
```

#### banks (题库表)
```sql
CREATE TABLE banks (
  id VARCHAR(255) PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  category VARCHAR(100),
  level VARCHAR(50),
  description TEXT,
  question_count INTEGER DEFAULT 0,
  score_config JSONB,
  usage_count INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_banks_category ON banks(category);
CREATE INDEX idx_banks_level ON banks(level);
```

#### questions (题目表)
```sql
CREATE TABLE questions (
  id VARCHAR(255) PRIMARY KEY,
  bank_id VARCHAR(255) NOT NULL,
  type VARCHAR(50) NOT NULL,
  content TEXT NOT NULL,
  options JSONB,
  answer JSONB,
  explanation TEXT,
  chapter VARCHAR(200),
  blanks JSONB,
  reference_answer TEXT,
  ai_grading_enabled BOOLEAN DEFAULT FALSE,
  tags JSONB,
  sort_order INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (bank_id) REFERENCES banks(id) ON DELETE CASCADE
);

CREATE INDEX idx_questions_bank_id ON questions(bank_id);
CREATE INDEX idx_questions_type ON questions(type);
CREATE INDEX idx_questions_sort_order ON questions(bank_id, sort_order);
CREATE INDEX idx_questions_tags ON questions USING GIN(tags);
```

#### tags (标签表)
```sql
CREATE TABLE tags (
  id VARCHAR(255) PRIMARY KEY,
  name VARCHAR(100) UNIQUE NOT NULL,
  color VARCHAR(50),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  usage_count INTEGER DEFAULT 0
);

CREATE INDEX idx_tags_name ON tags(name);
```

#### exams (考试表)
```sql
CREATE TABLE exams (
  id VARCHAR(255) PRIMARY KEY,
  bank_id VARCHAR(255) NOT NULL,
  title VARCHAR(255) NOT NULL,
  duration INTEGER NOT NULL,
  total_score INTEGER NOT NULL,
  pass_score INTEGER,
  pass_score_percent INTEGER,
  strategy VARCHAR(50) NOT NULL,
  selected_question_ids JSONB,
  status VARCHAR(50) DEFAULT 'PENDING',
  is_visible BOOLEAN DEFAULT TRUE,
  start_time TIMESTAMP,
  end_time TIMESTAMP,
  single_count INTEGER DEFAULT 0,
  multiple_count INTEGER DEFAULT 0,
  judge_count INTEGER DEFAULT 0,
  fill_in_blank_count INTEGER DEFAULT 0,
  short_answer_count INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (bank_id) REFERENCES banks(id) ON DELETE CASCADE
);

CREATE INDEX idx_exams_bank_id ON exams(bank_id);
CREATE INDEX idx_exams_status ON exams(status);
CREATE INDEX idx_exams_is_visible ON exams(is_visible);
```

#### exam_history (考试历史表)
```sql
CREATE TABLE exam_history (
  id VARCHAR(255) PRIMARY KEY,
  user_id VARCHAR(255) NOT NULL,
  exam_id VARCHAR(255) NOT NULL,
  exam_title VARCHAR(255),
  score NUMERIC(10,2),
  total_score INTEGER,
  pass_score INTEGER,
  time_used INTEGER,
  submit_time TIMESTAMP,
  bank_id VARCHAR(255),
  wrong_question_ids JSONB,
  user_answers JSONB,
  passed BOOLEAN,
  current_index INTEGER DEFAULT 0,
  is_finished BOOLEAN DEFAULT FALSE,
  exam_config JSONB,
  ordered_question_ids JSONB,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (exam_id) REFERENCES exams(id) ON DELETE CASCADE
);

CREATE INDEX idx_exam_history_user_id ON exam_history(user_id);
CREATE INDEX idx_exam_history_exam_id ON exam_history(exam_id);
CREATE INDEX idx_exam_history_submit_time ON exam_history(submit_time);
```

#### practice_records (练习记录表)
```sql
CREATE TABLE practice_records (
  id VARCHAR(255) PRIMARY KEY,
  user_id VARCHAR(255) NOT NULL,
  bank_id VARCHAR(255) NOT NULL,
  bank_name VARCHAR(255),
  type VARCHAR(50),
  question_type_filter JSONB,
  mode VARCHAR(50),
  count INTEGER,
  date DATE,
  current_index INTEGER DEFAULT 0,
  user_answers JSONB,
  is_custom BOOLEAN DEFAULT FALSE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (bank_id) REFERENCES banks(id) ON DELETE CASCADE
);

CREATE INDEX idx_practice_records_user_id ON practice_records(user_id);
CREATE INDEX idx_practice_records_bank_id ON practice_records(bank_id);
CREATE INDEX idx_practice_records_date ON practice_records(date);
```

#### srs_records (SRS复习记录表)
```sql
CREATE TABLE srs_records (
  id VARCHAR(255) PRIMARY KEY,
  user_id VARCHAR(255) NOT NULL,
  question_id VARCHAR(255) NOT NULL,
  bank_id VARCHAR(255) NOT NULL,
  next_review_date DATE,
  interval_days INTEGER DEFAULT 1,
  ease_factor NUMERIC(3,2) DEFAULT 2.5,
  review_count INTEGER DEFAULT 0,
  last_review_date DATE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (question_id) REFERENCES questions(id) ON DELETE CASCADE,
  FOREIGN KEY (bank_id) REFERENCES banks(id) ON DELETE CASCADE
);

CREATE INDEX idx_srs_records_user_id ON srs_records(user_id);
CREATE INDEX idx_srs_records_next_review_date ON srs_records(next_review_date);
```

#### discussions (讨论表)
```sql
CREATE TABLE discussions (
  id VARCHAR(255) PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  content TEXT NOT NULL,
  author_id VARCHAR(255) NOT NULL,
  author_name VARCHAR(100),
  question_id VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  last_activity_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  view_count INTEGER DEFAULT 0,
  like_count INTEGER DEFAULT 0,
  comment_count INTEGER DEFAULT 0,
  is_pinned BOOLEAN DEFAULT FALSE,
  is_hidden BOOLEAN DEFAULT FALSE,
  FOREIGN KEY (author_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (question_id) REFERENCES questions(id) ON DELETE SET NULL
);

CREATE INDEX idx_discussions_author_id ON discussions(author_id);
CREATE INDEX idx_discussions_question_id ON discussions(question_id);
CREATE INDEX idx_discussions_created_at ON discussions(created_at);
CREATE INDEX idx_discussions_is_pinned ON discussions(is_pinned);
```

#### comments (评论表)
```sql
CREATE TABLE comments (
  id VARCHAR(255) PRIMARY KEY,
  discussion_id VARCHAR(255) NOT NULL,
  parent_id VARCHAR(255),
  author_id VARCHAR(255) NOT NULL,
  author_name VARCHAR(100),
  content TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  like_count INTEGER DEFAULT 0,
  FOREIGN KEY (discussion_id) REFERENCES discussions(id) ON DELETE CASCADE,
  FOREIGN KEY (parent_id) REFERENCES comments(id) ON DELETE CASCADE,
  FOREIGN KEY (author_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE INDEX idx_comments_discussion_id ON comments(discussion_id);
CREATE INDEX idx_comments_parent_id ON comments(parent_id);
CREATE INDEX idx_comments_author_id ON comments(author_id);
```

#### discussion_likes (讨论点赞表)
```sql
CREATE TABLE discussion_likes (
  user_id VARCHAR(255) NOT NULL,
  discussion_id VARCHAR(255) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (user_id, discussion_id),
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (discussion_id) REFERENCES discussions(id) ON DELETE CASCADE
);
```

### 其他数据表

- **practical_tasks**: 实操题表
- **practical_records**: 实操记录表
- **mistakes**: 错题表
- **favorites**: 收藏表
- **notes**: 笔记表
- **daily_progress**: 每日进度表
- **system_config**: 系统配置表
- **system_config_kv**: 系统配置键值表
- **custom_fields**: 自定义字段表
- **login_logs**: 登录日志表
- **audit_logs**: 审计日志表
- **ai_analysis**: AI分析表

**总计**: 23个数据表，83个索引，19个JSONB字段

## 数据库连接池配置

```javascript
// db.js
import { Pool } from 'pg';

const pool = new Pool({
  host: process.env.DB_HOST || 'localhost',
  port: process.env.DB_PORT || 5432,
  database: process.env.DB_NAME || 'edumaster',
  user: process.env.DB_USER || 'edumaster_user',
  password: process.env.DB_PASSWORD,
  
  // 连接池配置
  min: 2,                    // 最小连接数
  max: 20,                   // 最大连接数
  idleTimeoutMillis: 30000,  // 空闲连接超时时间
  connectionTimeoutMillis: 2000, // 连接超时时间
});

// 错误处理
pool.on('error', (err, client) => {
  console.error('数据库连接池错误:', err);
});

export default pool;
```

## 环境配置

### 环境变量 (.env)
```bash
# 数据库配置
DB_HOST=postgres
DB_PORT=5432
DB_NAME=edumaster
DB_USER=edumaster_user
DB_PASSWORD=your_secure_password_here

# JWT Secret (用于用户认证)
JWT_SECRET=your_jwt_secret_here

# Server Port (后端服务端口)
PORT=3001

# DeepSeek API Key (可选，可在系统设置中配置)
DEEPSEEK_API_KEY=

# Node Environment
NODE_ENV=production
```

### Docker Compose 配置
```yaml
version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    container_name: edumaster_postgres
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=C"
    volumes:
      - ./postgres/data:/var/lib/postgresql/data
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: edumaster_api
    environment:
      NODE_ENV: production
      PORT: 3001
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: edumaster_nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api
    restart: unless-stopped
```

### Nginx 配置

#### 标准 Docker 部署配置

**注意**: 以下是标准 Docker 部署的 Nginx 配置（`nginx/nginx.conf`）。

⚠️ **生产环境**: 服务器使用宝塔面板管理 Nginx，配置文件位于 `/www/server/nginx/conf/nginx.conf`，项目的 `nginx/` 目录配置**未被使用**。

**Docker 部署配置** (`nginx/nginx.conf`):
```nginx
http {
    # HTTP 重定向到 HTTPS
    server {
        listen 80;
        server_name exammaster.zzzjl.com;
        return 301 https://$server_name$request_uri;
    }

    # HTTPS 服务器
    server {
        listen 443 ssl http2;
        server_name exammaster.zzzjl.com;

        # SSL 证书配置
        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # 前端静态文件
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
            
            # 缓存策略
            location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
                expires 1y;
                add_header Cache-Control "public, immutable";
            }
        }

        # API 代理
        location /api/ {
            proxy_pass http://api:3001;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
        }
    }
}
```

#### 生产环境宝塔面板 Nginx 配置 ⚠️ 重要

生产服务器的实际配置由宝塔面板管理，配置要点：

**1. 反向代理配置**
- 目标地址: `http://127.0.0.1:3001`
- 发送域名: `$host`
- 启用缓存、WebSocket 支持
- 配置路径: 宝塔面板 → 网站 → exammaster.zzzjl.com → 反向代理

**2. SSL 证书**
- 由宝塔面板管理
- 自动续期 Let's Encrypt 证书
- 配置路径: 宝塔面板 → 网站 → exammaster.zzzjl.com → SSL

**3. 静态文件**
- 网站目录: `/www/wwwroot/exammaster.zzzjl.com/dist`
- 默认文档: `index.html`
- 配置路径: 宝塔面板 → 网站 → exammaster.zzzjl.com → 网站目录

**4. 配置管理**
- 通过宝塔面板 Web 界面管理
- 配置文件: `/www/server/nginx/conf/nginx.conf`
- 重载配置: `nginx -s reload` 或宝塔面板重启

**5. 日志文件**
- 访问日志: `/www/wwwlogs/exammaster.zzzjl.com.log`
- 错误日志: `/www/wwwlogs/exammaster.zzzjl.com.error.log`

**配置示例**（宝塔面板生成的配置片段）:
```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name exammaster.zzzjl.com;
    
    # SSL 证书（宝塔面板管理）
    ssl_certificate /www/server/panel/vhost/cert/exammaster.zzzjl.com/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/exammaster.zzzjl.com/privkey.pem;
    
    # 网站根目录
    root /www/wwwroot/exammaster.zzzjl.com/dist;
    index index.html;
    
    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**重要提示**:
- ⚠️ 项目 `nginx/` 目录的配置文件**不会被使用**
- ⚠️ 所有 Nginx 配置修改需要在宝塔面板中进行
- ⚠️ SSL 证书由宝塔面板自动管理，无需手动配置
- ✅ 修改配置后记得重启 Nginx 服务

详细配置说明请参考: [Nginx配置说明.md](Nginx配置说明.md) 和 [服务器实际架构说明.md](服务器实际架构说明.md)

## 安全特性

### 1. 认证与授权
- JWT Token 认证（7天有效期）
- 基于角色的权限控制 (RBAC)
- 密码 bcrypt 加密 (10 轮)
- 多标签页登录状态同步

### 2. API 安全
- CORS 跨域保护（限制来源域名）
- 请求体大小限制 (50MB)
- SQL 注入防护（参数化查询）
- XSS 防护（前端输入验证）
- 认证中间件保护所有敏感接口

### 3. 数据安全
- 密码不可逆加密
- 敏感信息不返回前端
- PostgreSQL 访问控制（pg_hba.conf）
- 密码加密算法（scram-sha-256）
- API Key 加密存储

### 4. 网络安全
- Nginx 反向代理
- HTTPS 加密传输（Let's Encrypt SSL）
- HTTP 自动重定向到 HTTPS
- 安全响应头配置

### 5. 容器安全
- Docker 容器隔离
- 健康检查机制
- 自动重启策略
- 数据卷权限控制

## 性能优化

### 1. 前端优化
- Vite 构建优化
- 代码分割（按路由）
- 静态资源缓存 (1年)
- Gzip 压缩
- 图片懒加载
- 虚拟滚动（大列表）

### 2. 后端优化
- 数据库连接池（最小2，最大20）
- JSONB 字段优化
- 查询性能优化
- 批量操作使用事务
- 分页查询实现

### 3. 数据库优化
- 83个索引优化查询
- JSONB 字段使用 GIN 索引
- 外键约束
- 事务处理
- 查询计划分析（EXPLAIN ANALYZE）

### 4. 缓存策略
- 静态资源浏览器缓存
- Nginx 缓存配置
- 连接池复用

## 监控与日志

### 日志系统
- **应用日志**: Winston 日志库
- **日志轮转**: 按天轮转
- **日志级别**: info, warn, error
- **日志文件**: 
  - `logs/app-YYYY-MM-DD.log` - 应用日志
  - `logs/error-YYYY-MM-DD.log` - 错误日志

### 监控指标
- 数据库连接池状态
- 查询性能监控
- 慢查询日志
- 容器健康检查
- 资源使用监控

### Docker 监控命令

#### 本地开发/测试环境（标准 Docker Compose）

```bash
# 查看容器状态
docker-compose ps

# 查看容器日志
docker-compose logs -f
docker-compose logs -f api
docker-compose logs -f postgres
docker-compose logs -f nginx

# 查看容器资源使用
docker stats

# 进入容器
docker-compose exec api sh
docker-compose exec postgres psql -U edumaster_user -d edumaster
docker-compose exec nginx sh

# 重启服务
docker-compose restart
docker-compose restart api
docker-compose restart postgres
```

#### 生产环境（宝塔面板 + Docker）⚠️ 重要

**Docker 容器管理**（使用 `docker` 命令）:
```bash
# 查看容器状态
docker ps
docker ps -a  # 包含停止的容器

# 查看容器日志
docker logs -f edumaster_api
docker logs -f edumaster_postgres
docker logs --tail=100 edumaster_api  # 查看最后 100 行

# 查看容器资源使用
docker stats
docker stats edumaster_api edumaster_postgres

# 进入容器
docker exec -it edumaster_api sh
docker exec -it edumaster_postgres psql -U edumaster_user -d edumaster

# 重启容器
docker restart edumaster_api
docker restart edumaster_postgres

# 停止/启动容器
docker stop edumaster_api
docker start edumaster_api
```

**Nginx 管理**（宝塔面板）:
```bash
# 查看 Nginx 状态
systemctl status nginx
# 或
/etc/init.d/nginx status

# 重启 Nginx
systemctl restart nginx
# 或
/etc/init.d/nginx restart
# 或
nginx -s reload  # 重新加载配置

# 测试 Nginx 配置
nginx -t

# 查看 Nginx 日志
tail -f /www/wwwlogs/exammaster.zzzjl.com.log
tail -f /www/wwwlogs/exammaster.zzzjl.com.error.log
```

**注意事项**:
- ⚠️ 生产环境**不使用** `docker-compose` 命令
- ⚠️ Nginx 配置在宝塔面板中管理，不在项目 `nginx/` 目录
- ⚠️ 修改 Nginx 配置后需要通过宝塔面板或命令行重启 Nginx
- ✅ API 和数据库容器使用 `docker` 命令管理

## 备份策略

### 自动备份
```bash
# 备份脚本（服务器脚本/backup.sh）
#!/bin/bash
BACKUP_DIR="/www/wwwroot/exammaster.zzzjl.com/backups"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/edumaster_backup_$DATE.sql.gz"

# 执行备份
docker-compose exec -T postgres pg_dump -U edumaster_user edumaster | gzip > $BACKUP_FILE

# 保留最近7天的备份
find $BACKUP_DIR -name "edumaster_backup_*.sql.gz" -mtime +7 -delete

echo "备份完成: $BACKUP_FILE"
```

### 定时备份（crontab）
```bash
# 每天凌晨2点自动备份
0 2 * * * cd /www/wwwroot/exammaster.zzzjl.com && ./服务器脚本/backup.sh
```

### 数据恢复
```bash
# 恢复脚本（服务器脚本/restore.sh）
#!/bin/bash
BACKUP_FILE=$1

if [ -z "$BACKUP_FILE" ]; then
    echo "用法: ./restore.sh <backup_file>"
    exit 1
fi

# 停止 API 服务
docker-compose stop api

# 恢复数据库
gunzip < $BACKUP_FILE | docker-compose exec -T postgres psql -U edumaster_user edumaster

# 重启服务
docker-compose start api

echo "恢复完成"
```

## 部署流程

### 初次部署
```bash
# 1. 克隆代码
git clone https://github.com/xmwengxing/EduMaster_postgresql.git
cd EduMaster_postgresql

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，设置数据库密码和 JWT Secret

# 3. 构建前端
npm install
npm run build

# 4. 启动 Docker 服务
docker-compose up -d

# 5. 查看服务状态
docker-compose ps

# 6. 查看日志
docker-compose logs -f
```

### 更新部署
```bash
# 1. 拉取最新代码
git pull origin main

# 2. 备份数据库
./服务器脚本/backup.sh

# 3. 重新构建前端（如有更新）
npm install
npm run build

# 4. 重启服务
docker-compose down
docker-compose up -d --build

# 5. 验证服务
docker-compose ps
curl https://exammaster.zzzjl.com/api/health
```

### CI/CD 自动部署
- **GitHub Actions**: 自动构建和测试
- **自动部署**: 推送到 main 分支自动部署
- **健康检查**: 每小时自动检查服务状态
- **失败回滚**: 部署失败自动回滚到上一版本

## 故障排查

### 常见问题

#### 1. 服务无法启动
```bash
# 查看容器状态
docker-compose ps

# 查看容器日志
docker-compose logs

# 检查端口占用
netstat -tlnp | grep -E '80|443|3001|5432'

# 重启服务
docker-compose restart
```

#### 2. 数据库连接失败
```bash
# 检查 PostgreSQL 容器
docker-compose logs postgres

# 检查环境变量
cat .env

# 测试数据库连接
docker-compose exec postgres psql -U edumaster_user -d edumaster -c "SELECT 1;"

# 重启数据库
docker-compose restart postgres
```

#### 3. API 返回 500 错误
```bash
# 查看 API 日志
docker-compose logs -f api

# 查看应用日志
tail -f logs/error-$(date +%Y-%m-%d).log

# 重启 API 服务
docker-compose restart api
```

#### 4. 前端无法访问
```bash
# 检查 Nginx 配置
docker-compose exec nginx nginx -t

# 查看 Nginx 日志
docker-compose logs -f nginx

# 检查前端文件
ls -la dist/

# 重启 Nginx
docker-compose restart nginx
```

#### 5. 磁盘空间不足
```bash
# 查看磁盘使用
df -h

# 清理 Docker 镜像
docker system prune -a

# 清理旧日志
find logs/ -name "*.log" -mtime +7 -delete

# 清理旧备份
find backups/ -name "*.sql.gz" -mtime +7 -delete
```

### 性能问题排查

#### 1. 查询性能分析
```bash
# 运行性能分析脚本
node scripts/analyze-query-performance.js

# 查看慢查询
docker-compose exec postgres psql -U edumaster_user -d edumaster -c "
SELECT query, calls, total_time, mean_time 
FROM pg_stat_statements 
ORDER BY mean_time DESC 
LIMIT 10;"
```

#### 2. 连接池监控
```bash
# 查看连接池状态
docker-compose exec api node -e "
const pool = require('./db.js').default;
console.log('Total:', pool.totalCount, 'Idle:', pool.idleCount, 'Waiting:', pool.waitingCount);
"
```

## 运维脚本

### 可用脚本

#### 本地开发脚本（scripts/）
- `migrate.js` - 数据迁移脚本
- `verify-migration.js` - 迁移验证
- `verify-postgres-schema.js` - 架构验证
- `analyze-query-performance.js` - 查询性能分析
- `generate-secure-passwords.js` - 生成安全密码
- `verify-security-config.js` - 安全配置验证
- `clear-postgres.js` - 清空数据库
- `final-verification.js` - 最终系统验证
- `README.md` - 脚本说明文档

#### 服务器脚本（服务器脚本/）
- `backup.sh` - 数据库备份
- `restore.sh` - 数据库恢复
- `monitor-database.sh` - 数据库监控
- `deploy.sh` - 自动部署
- `rollback.sh` - 回滚脚本
- `cleanup-and-optimize.sh` - 清理优化

### 使用示例

```bash
# 数据库备份
./服务器脚本/backup.sh

# 数据库恢复
./服务器脚本/restore.sh backups/edumaster_backup_20260123.sql.gz

# 数据库监控
./服务器脚本/monitor-database.sh

# 自动部署
./服务器脚本/deploy.sh

# 性能分析
node scripts/analyze-query-performance.js

# 安全验证
node scripts/verify-security-config.js

# 最终验证
node scripts/final-verification.js
```

## 系统要求

### 服务器配置
- **最低配置**: 2核 CPU, 2GB RAM, 20GB 存储
- **推荐配置**: 4核 CPU, 4GB RAM, 50GB 存储
- **操作系统**: CentOS 7+ / Ubuntu 20.04+ / Debian 10+

### 软件要求
- **Docker**: 20.10+
- **Docker Compose**: 1.29+
- **Node.js**: 18.0+ (本地开发)
- **PostgreSQL**: 14+ (容器内)

## 重要提示

1. ⚠️ 首次登录后请立即修改默认密码
2. 📦 定期备份数据库文件（建议每天）
3. 📊 监控服务器资源使用情况
4. 🔄 及时更新系统和依赖包
5. 🔒 妥善保管 API Key 和 JWT Secret
6. 📝 定期查看日志文件
7. 🧪 在测试环境验证后再部署到生产环境
8. 🔐 定期更新 SSL 证书

## 快速参考

### 常用命令
```bash
# 启动所有服务
docker-compose up -d

# 停止所有服务
docker-compose down

# 重启服务
docker-compose restart

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 备份数据库
./服务器脚本/backup.sh

# 恢复数据库
./服务器脚本/restore.sh <backup_file>

# 进入数据库
docker-compose exec postgres psql -U edumaster_user -d edumaster

# 查看数据库大小
docker-compose exec postgres psql -U edumaster_user -d edumaster -c "SELECT pg_size_pretty(pg_database_size('edumaster'));"
```

### 访问地址
- **生产环境**: https://exammaster.zzzjl.com
- **API 端点**: https://exammaster.zzzjl.com/api
- **健康检查**: https://exammaster.zzzjl.com/api/health
- **GitHub 仓库**: https://github.com/xmwengxing/EduMaster_postgresql

### 默认管理员账号
- **账号**: admin
- **密码**: admin
- **角色**: 超级管理员

## 文档清单

### 核心文档
1. **README.md** - 项目说明
2. **技术文档.md** - 技术文档（本文档）
3. **DEPLOYMENT_GUIDE.md** - 部署指南
4. **QUICK_REFERENCE.md** - 快速参考指南
5. **SYSTEM_DELIVERY_REPORT.md** - 系统交付报告
6. **DELIVERY_CHECKLIST.md** - 交付清单
7. **FINAL_SYSTEM_VERIFICATION.md** - 最终验证报告

### 规范文档
8. **.kiro/specs/postgresql-migration-deployment/requirements.md** - 需求文档
9. **.kiro/specs/postgresql-migration-deployment/design.md** - 设计文档
10. **.kiro/specs/postgresql-migration-deployment/tasks.md** - 任务清单

### 配置文档
11. **postgres/README.md** - PostgreSQL 配置说明
12. **postgres/SCHEMA_VERIFICATION.md** - 架构验证文档
13. **postgres/SECURITY_HARDENING.md** - 安全加固文档
14. **nginx/README.md** - Nginx 配置说明
15. **scripts/README.md** - 脚本说明文档

### GitHub 文档
16. **.github/README.md** - GitHub Actions 说明
17. **.github/workflows/ci-cd.yml** - CI/CD 工作流
18. **.github/workflows/health-check.yml** - 健康检查工作流

## 更新日志

### v4.0.0 (2026-01-26) - 模块化架构版本 ✨
- ✨ **模块化重构**: 将 4100+ 行单体代码重构为 50+ 个模块化文件
- ✨ **12 个业务模块**: auth, user, bank, question, practice, srs, exam, practical, discussion, ai, tag, admin, system
- ✨ **三层架构**: 路由层 → 控制器层 → 服务层
- ✨ **492 个测试用例**: 单元测试 + 集成测试，100% 通过率
- ✨ **测试覆盖率**: ≥70%
- ✨ **自动化部署**: 快速部署脚本，支持备份回滚
- ✨ **完整文档体系**: 规范文档、技术文档、API 文档
- 🔧 **性能优化**: 部署时间从 ~10分钟 → <5分钟
- 🔧 **代码质量**: 代码可维护性显著提升
- 🔄 **向后兼容**: 所有 API 保持不变，无需前端修改
- 📚 **文档完善**: 15+ 个文档，包含架构设计、部署指南、运维手册

### v3.0.0 (2026-01-23) - PostgreSQL 版本
- ✨ 数据库迁移到 PostgreSQL 14
- ✨ Docker 容器化部署
- ✨ Nginx 反向代理 + HTTPS
- ✨ 数据库连接池优化
- ✨ 83个索引优化查询性能
- ✨ 19个JSONB字段优化
- ✨ Winston 日志系统
- ✨ 自动备份和恢复机制
- ✨ CI/CD 自动化部署
- ✨ 健康检查和监控
- ✨ 安全加固（CORS、SSL、密码加密）
- 🔧 查询性能优化（< 50ms）
- 🔧 并发性能优化（< 150ms）
- 🔧 分页查询实现
- 🔧 批量操作优化
- 📚 完整的文档体系

### v2.0.0 (2026-01-09) - SQLite 版本
- ✨ 新增填空题和简答题支持
- ✨ 新增题目标签管理系统
- ✨ 新增讨论社区功能
- ✨ 新增 SRS 智能复习算法
- ✨ 新增多 AI 服务商支持
- ✨ 新增富文本编辑器
- ✨ 新增主题定制功能
- 🔧 优化权限管理系统
- 🔧 优化练习模式交互

### v1.0.0 (2026-01-07) - 初始版本
- 🎉 初始版本发布
- ✨ 基础题库管理功能
- ✨ 练习和考试功能
- ✨ 实操题功能
- ✨ AI 讲解功能
- ✨ 用户权限管理

---

**文档版本**: 4.0.0  
**最后更新**: 2026-01-26  
**维护者**: EduMaster 开发团队
