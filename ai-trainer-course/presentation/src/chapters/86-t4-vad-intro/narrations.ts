export const narrations: string[] = [
  "拿到了规格对齐的原始音频之后，我们不能直接把整段录音丢给模型。一套完整的语音数据预处理流程分为两步。第一步，切除无效静音。第二步，把长录音切成短片段。这两步加在一起，在工程上叫做VAD加切片流水线。",
  "先说VAD。VAD全称是Voice Activity Detection，静音检测。它的作用是自动识别一段录音中哪一段有人说话、哪一段是静音或者只有背景噪音。你拿到一段10分钟的呼叫中心录音，实际上客户和坐席真正说话的时间可能只有6分钟，开头结尾的长段等候音和中间的沉默间隙占了剩余的4分钟。如果这些静音片段不去掉，它们会白白消耗标注员的时间和注意力，标注员不停地在等待下一句话的过程中消耗精力，疲劳度和错误率都会显著上升。",
  "VAD的技术实现已经非常成熟。最常用的工具是WebRTC的VAD模块，它是Google开源的一套轻量级静音检测算法，可以实时处理，对CPU的消耗极低。Python里可以直接调用webrtcvad这个库，你只需要设置一个敏感度参数，算法就会自动在音频流中标记出语音段和非语音段。一般来说，敏感度设置为2或3就能满足大多数语音场景的需求。",
  "VAD做完之后，你得到的是一系列有语音的片段。但这些片段可能仍然很长，一个片段可能包含客户连续说了30秒的话。大多数ASR模型和语音情感识别模型对输入长度有严格限制，通常只能处理几秒到十几秒的短音频。所以下一步我们必须把长的语音片段切成短句。但这个切法非常有讲究。一刀切错位置，模型就彻底懵了。接下来我们看怎么切片。",
];
