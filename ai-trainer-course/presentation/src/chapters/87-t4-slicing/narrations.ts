export const narrations: string[] = [
  "切片的第一个原则是——千万不要从句子中间一刀切。举个例子你就明白了。用户说了一句话：我要退款，但是我不知道怎么操作。如果你在退和款之间或者操和作之间下刀，模型收到的分别是我要退和款但是我不知道怎么操作，它完全无法理解这个句子的完整语义。这种从词语中间切断导致的语义碎片化，是语音预处理中最常见的低级错误之一。",
  "正确的做法叫做语义级平滑切片。具体操作分三步。第一步，先用VAD检测出所有静音间隔，这些间隔是天然的切分候选点。第二步，对于静音间隔之间仍然过长的语音段，结合ASR初步转写的结果或者人工预判的标点位置，在语义完整的位置——也就是句末或自然停顿处——进行切分。第三步，最关键的一点——在所有切分点处保留一个几百毫秒的重叠区，也就是Overlap。",
  "为什么要保留Overlap？因为如果切得太干净，相邻片段之间的边界处可能会丢失几个毫秒的音频，这几个毫秒可能正好是一个字的声母或者韵尾。丢掉这一点信号，那个字就听不清了，标注员就会标注错误。有了一个200到500毫秒的Overlap，相邻片段之间有足够的安全缓冲区，声音就不会被吞掉。",
  "另外还有一个重要的参数是切片的目标时长。根据大多数ASR模型的输入限制，每个切片建议控制在10到15秒之间。太短了没有足够的语义上下文，标注员无法准确判断句子的含义。太长了超过模型的输入窗口，模型的性能会急剧下降。10到15秒正好是一句到两句话的时长，语义信息足够完整，同时也在绝大多数模型的输入限制之内。",
  "好，VAD和切片的基本方法讲完了。接下来我们要面对语音数据处理中最让人头疼的一个难题——当两个人的声音在时间上重叠的时候，到底是该保留还是该丢弃？这个判断不能凭感觉做，必须根据你的业务目标来定。我们来看一种最典型的重叠音场景。",
];
