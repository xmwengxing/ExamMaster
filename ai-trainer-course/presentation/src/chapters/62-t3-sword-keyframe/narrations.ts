export const narrations: string[] = [
  "第二剑，I帧提取。这个策略利用了视频编码本身的一个技术特性。你可能不知道，现在几乎所有的视频在存储和传输时都是被压缩过的。视频压缩编码有一个核心机制——它不存储每一帧的完整画面，而是只存储相邻帧之间的差异。这种差异帧叫P帧和B帧，它们非常小，但信息不完整，没法直接当图片用。但压缩算法会定期保留一些完整的画面作为参考基准，这些就是I帧，也叫关键帧。",
  "I帧本身就是一张完整的JPEG图片。你不需要重新解码视频，直接从视频文件里把I帧提取出来就行了。这个操作在FFmpeg里只需要加一个skip_frame参数即可。但I帧提取有一个天然的局限性：I帧是编码器自动插入的，它的分布不是均匀的。通常一个视频里每2到5秒才有一个I帧，具体取决于编码参数的设置。这意味着你无法精确控制抽取的位置和密度。",
  "所以I帧提取最适合什么场景？适合对时间精度要求不高、只需要做宏观统计的任务。典型例子是商场客流量统计。你不关心张三在几点几分几秒走过第三个货架，你只关心今天下午有多少人经过了某个区域。这种情况下，每几秒一张的I帧完全够用，而且提取速度极快，节省了大量解码时间。",
  "但是，如果你的业务需要精确的时间定位，比如安防事件追溯中需要精确到秒的目标识别，I帧策略就不够用了。你需要更高精度、更事件驱动的抽帧方式。这就是我们的第三剑。",
];
