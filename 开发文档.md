# EduMaster 全栈刷题系统 - 开发文档

## 1. 项目概述

EduMaster是一个基于React+TypeScript+Node.js+PostgreSQL的全栈刷题系统，支持题库管理、练习模式、考试系统、用户管理、数据分析等功能。项目采用Docker容器化部署，具有良好的可扩展性和维护性。

## 2. 技术栈

### 前端
- React 19 + TypeScript
- Vite 构建工具
- Ant Design 5.x UI组件库
- React Router 路由管理

### 后端
- Node.js 18+
- Express 5.x 框架
- PostgreSQL 14+ 数据库
- pg 库进行数据库连接
- JWT 身份认证
- bcryptjs 密码加密
- Winston 日志系统

### 部署
- Docker + Docker Compose
- GitHub Actions CI/CD
- Nginx 反向代理

## 3. 目录结构

```
├── .github/           # GitHub CI/CD 配置
├── components/        # React 组件
├── docs/              # 项目文档
├── nginx/             # Nginx 配置
├── pages/             # 页面组件
│   ├── Admin/         # 管理员页面
│   ├── Registration/  # 注册相关页面
│   └── Student/       # 学生页面
├── postgres/          # 数据库配置和迁移
├── public/            # 静态资源
├── scripts/           # 工具脚本
├── src/               # 后端源码
│   ├── config/        # 配置文件
│   ├── controllers/   # 控制器层
│   ├── middleware/    # 中间件
│   ├── routes/        # 路由
│   ├── services/      # 服务层
│   └── utils/         # 工具函数
├── tests/             # 测试文件
├── types/             # TypeScript 类型定义
├── uploads/           # 文件上传目录
├── App.tsx            # React 应用入口
├── Dockerfile         # Docker 构建文件
├── docker-compose.yml # Docker Compose 配置
├── index.tsx          # React 渲染入口
├── package.json       # 项目依赖
├── server.js          # 后端服务入口
└── tsconfig.json      # TypeScript 配置
```

## 4. 核心模块

### 4.1 用户认证
- JWT token 生成和验证
- 密码哈希和验证
- 权限控制（学生/管理员）

### 4.2 题库管理
- 题库 CRUD 操作
- 题目分类和标签
- 题目导入导出
- 题库转换工具

### 4.3 练习系统
- 随机练习模式
- 错题集管理
- 收藏题目
- 学习进度跟踪

### 4.4 考试系统
- 考试发布和管理
- 在线考试和自动评分
- 考试历史记录

### 4.5 注册系统
- 教育类注册
- 职业类注册
- 材料上传和审核

### 4.6 系统管理
- 用户管理
- 日志管理
- 系统配置
- 数据备份和恢复

## 5. 数据库设计

### 5.1 核心表
- `users`: 用户信息
- `question_banks`: 题库信息
- `questions`: 题目内容
- `exams`: 考试信息
- `exam_records`: 考试记录
- `practice_records`: 练习记录
- `mistakes`: 错题集
- `favorites`: 收藏夹
- `srs_records`: 学习进度记录

### 5.2 数据类型
- 使用 JSONB 存储复杂结构数据（如题目选项、用户答案等）
- 使用 UUID 作为主键
- 使用 TIMESTAMP 记录时间戳

## 6. API 结构

### 6.1 认证 API
- `POST /api/auth/login`: 用户登录
- `POST /api/auth/logout`: 用户登出
- `GET /api/auth/refresh`: 刷新 token

### 6.2 用户 API
- `GET /api/user/profile`: 获取用户信息
- `PUT /api/user/profile`: 更新用户信息
- `POST /api/user/change-password`: 修改密码

### 6.3 题库 API
- `GET /api/banks`: 获取题库列表
- `POST /api/banks`: 创建题库
- `PUT /api/banks/:id`: 更新题库
- `DELETE /api/banks/:id`: 删除题库

### 6.4 题目 API
- `GET /api/questions`: 获取题目列表
- `POST /api/questions`: 创建题目
- `PUT /api/questions/:id`: 更新题目
- `DELETE /api/questions/:id`: 删除题目

### 6.5 练习 API
- `GET /api/practice`: 获取练习记录
- `POST /api/practice`: 创建练习记录
- `PUT /api/practice/:id`: 更新练习记录

### 6.6 考试 API
- `GET /api/exams`: 获取考试列表
- `POST /api/exams`: 创建考试
- `PUT /api/exams/:id`: 更新考试
- `DELETE /api/exams/:id`: 删除考试

### 6.7 注册 API
- `POST /api/registrations/education`: 教育类注册
- `POST /api/registrations/vocational`: 职业类注册
- `GET /api/registrations/search`: 搜索注册记录

### 6.8 管理员 API
- `GET /api/admin/students`: 获取学生列表
- `POST /api/admin/students`: 创建学生
- `PUT /api/admin/students/:id`: 更新学生
- `DELETE /api/admin/students/:id`: 删除学生
- `GET /api/admin/login-logs`: 获取登录日志
- `GET /api/admin/audit-logs`: 获取审计日志

## 7. 开发环境配置

### 7.1 安装依赖
```bash
npm install
```

### 7.2 启动开发服务器
```bash
npm run start:dev  # 同时启动前后端开发服务器
npm run dev:client # 仅启动前端开发服务器
npm run dev:server # 仅启动后端开发服务器
```

### 7.3 构建生产版本
```bash
npm run build
```

### 7.4 运行测试
```bash
npm test       # 运行所有测试
npm run test:watch  # 监听模式运行测试
npm run test:coverage # 生成测试覆盖率报告
```

### 7.5 Docker 部署
```bash
docker-compose up -d
```

## 8. 关键文件说明

### 8.1 后端入口
- `server.js`: 后端服务入口，配置 Express 应用和中间件

### 8.2 数据库连接
- `db.js`: PostgreSQL 数据库连接池配置和查询工具函数

### 8.3 路由管理
- `src/routes/index.js`: 路由聚合器，注册所有 API 路由

### 8.4 状态管理
- `store.ts`: React 应用状态管理，使用 React Hooks 实现

### 8.5 认证服务
- `src/services/auth.service.js`: 认证服务，处理登录、密码验证等

### 8.6 文件上传
- `src/services/chunked-upload.service.ts`: 分片上传服务，支持大文件上传

### 8.7 错误处理
- `src/middleware/errorHandler.js`: 全局错误处理中间件
- `src/services/error-logger.service.ts`: 错误日志记录服务

## 9. 开发规范

### 9.1 代码风格
- 使用 TypeScript 进行类型检查
- 遵循 ESLint 和 Prettier 代码规范
- 使用双引号和 2 空格缩进

### 9.2 命名规范
- 文件命名：使用小驼峰命名法
- 变量命名：使用小驼峰命名法
- 函数命名：使用小驼峰命名法
- 类命名：使用大驼峰命名法

### 9.3 数据库规范
- 使用 UUID 作为主键
- 表名使用复数形式
- 字段名使用下划线分隔

## 10. 监控与维护

### 10.1 日志系统
- 使用 Winston 记录应用日志
- 日志文件按日期分割
- 支持不同日志级别（error, warn, info, debug）

### 10.2 性能监控
- 慢查询日志记录
- 数据库连接池监控
- API 响应时间监控

### 10.3 安全措施
- JWT token 认证
- 密码哈希存储
- CORS 配置
- SQL 注入防护

## 11. 版本控制

- 使用 Git 进行版本控制
- 遵循 Git Flow 工作流
- 主分支：main
- 开发分支：develop
- 特性分支：feature/
- 修复分支：bugfix/

## 12. 常见问题

### 12.1 数据库连接问题
- 检查 .env 文件中的数据库配置
- 确保 PostgreSQL 服务正常运行
- 检查数据库用户权限

### 12.2 文件上传问题
- 检查上传目录权限
- 确保服务器有足够的磁盘空间
- 检查文件大小限制

### 12.3 性能问题
- 优化数据库查询
- 使用缓存减少数据库访问
- 优化前端渲染性能

---

本文档旨在为 AI 大模型提供快速了解项目结构和开发流程的参考。如需更详细的信息，请参考项目中的具体文件和代码注释。