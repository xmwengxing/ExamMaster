# 数据库配置说明

## ✅ 已完成的配置

1. **PostgreSQL 18 已安装并运行**
   - 服务名称：postgresql-x64-18
   - 监听端口：**5433**（因为 5432 被 Docker 占用）
   - 数据目录：D:\Program Files\PostgreSQL\18\data

2. **Node.js 依赖已安装**
   - ✅ pg (node-postgres) - PostgreSQL 驱动
   - ✅ vitest - 测试框架
   - ✅ fast-check - 属性测试库
   - ❌ sqlite3 - 已移除

## 📋 下一步操作

### 方法 1：自动设置（推荐）

运行自动设置脚本：

```powershell
# 在 PowerShell 中执行
.\setup-database.ps1
```

脚本会自动：
- 检查 PostgreSQL 服务状态
- 创建 edumaster 数据库
- 创建 edumaster_user 用户
- 配置权限

### 方法 2：手动设置

1. **打开 psql 命令行工具**
   ```powershell
   # 方式 1：使用完整路径
   & "D:\Program Files\PostgreSQL\18\bin\psql.exe" -h localhost -p 5433 -U postgres
   
   # 方式 2：如果已添加到 PATH
   psql -h localhost -p 5433 -U postgres
   ```

2. **执行 SQL 脚本**
   ```sql
   -- 创建数据库
   CREATE DATABASE edumaster WITH ENCODING 'UTF8';
   
   -- 创建用户
   CREATE USER edumaster_user WITH PASSWORD 'edumaster_password_2024';
   
   -- 授予权限
   GRANT ALL PRIVILEGES ON DATABASE edumaster TO edumaster_user;
   
   -- 连接到 edumaster 数据库
   \c edumaster
   
   -- 授予 schema 权限
   GRANT ALL ON SCHEMA public TO edumaster_user;
   ```

3. **更新 .env 文件**
   ```env
   DB_HOST=localhost
   DB_PORT=5433
   DB_NAME=edumaster
   DB_USER=edumaster_user
   DB_PASSWORD=edumaster_password_2024
   
   JWT_SECRET=your_jwt_secret_key_change_this_in_production
   ```

## 🧪 验证配置

运行测试脚本验证数据库连接：

```bash
node test-db-connection.js
```

如果看到 "✅ 数据库连接成功！"，说明配置正确。

## 📝 重要提示

1. **端口配置**
   - PostgreSQL 使用端口 **5433**（不是默认的 5432）
   - 所有配置文件和连接字符串都需要使用 5433

2. **密码安全**
   - 默认密码：`edumaster_password_2024`
   - **生产环境请务必修改为强密码**
   - 不要将 .env 文件提交到 Git

3. **Docker 端口冲突**
   - 5432 端口被 Docker 占用
   - 如果需要使用 5432，请停止 Docker 中的 PostgreSQL 容器

## 🔧 故障排查

### PostgreSQL 服务无法启动
```powershell
# 检查服务状态
Get-Service postgresql-x64-18

# 查看日志
Get-Content "D:\Program Files\PostgreSQL\18\data\log\*.log" -Tail 50
```

### 连接被拒绝
- 检查服务是否运行
- 检查端口是否正确（5433）
- 检查防火墙设置

### 密码认证失败
- 确认 .env 文件中的密码正确
- 确认用户已创建
- 检查 pg_hba.conf 配置

## 📚 相关文件

- `POSTGRESQL_SETUP.md` - 详细的 PostgreSQL 安装指南
- `setup-database.sql` - 数据库初始化 SQL 脚本
- `setup-database.ps1` - 自动设置 PowerShell 脚本
- `test-db-connection.js` - 数据库连接测试脚本
- `.env` - 环境变量配置文件

## ✅ 完成检查清单

- [x] PostgreSQL 18 已安装
- [x] PostgreSQL 服务正在运行（端口 5433）
- [x] Node.js 依赖已安装（pg, vitest, fast-check）
- [ ] edumaster 数据库已创建
- [ ] edumaster_user 用户已创建
- [ ] .env 文件已配置
- [ ] 数据库连接测试通过

完成上述步骤后，即可继续执行任务 2：创建 PostgreSQL 数据库架构。
