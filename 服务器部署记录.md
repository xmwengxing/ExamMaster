# 服务器部署记录

## 服务器信息
- **IP**: 47.104.173.139
- **系统**: CentOS 7.8
- **域名**: exammaster.zzzjl.com
- **部署路径**: /www/wwwroot/exammaster.zzzjl.com

---

## 任务 14: 服务器准备

### 检查方式

请手动执行以下命令来检查服务器环境：

```bash
# 双击运行
check-server.bat
```

或者直接 SSH 连接：

```bash
ssh root@47.104.173.139
```

然后手动检查：

```bash
# 1. 检查 Docker
docker --version
systemctl status docker

# 2. 检查 Docker Compose
docker-compose --version

# 3. 检查 Git
git --version

# 4. 检查端口
ss -tuln | grep -E ':(80|443|5432) '

# 5. 检查目录
ls -la /www/wwwroot

# 6. 检查磁盘
df -h /
```

### 检查结果

**日期**: 2025-01-22

#### 系统信息
- ✅ 操作系统: CentOS Linux 7 (Core)

#### Docker
- [x] 已安装，版本: **26.1.4**
- [x] Docker 服务: **运行中**

#### Docker Compose
- [x] 已安装（插件方式）
- 使用命令: `docker compose` (V2)

#### Git
- [x] 已安装，版本: **1.8.3.1**

#### 端口状态
- [x] 端口 80: **已被占用**（其他网站使用）
- [x] 端口 443: **可用**
- [x] 端口 5432: **可用**

#### 目录
- [x] /www/wwwroot/exammaster.zzzjl.com **已创建**
- [x] 权限: 755

#### 磁盘空间
- 可用空间: **55GB**
- [x] 充足（>10GB）

#### 防火墙
- [x] 端口 443 已开放
- [x] 防火墙规则已生效

### 需要安装的软件

如果缺少软件，请在服务器上执行：

```bash
# 上传安装脚本
# 在本地执行：
scp scripts/install-server-dependencies.sh root@47.104.173.139:/tmp/

# 在服务器上执行：
ssh root@47.104.173.139
chmod +x /tmp/install-server-dependencies.sh
/tmp/install-server-dependencies.sh
```

### 完成状态

- [x] 环境检查完成
- [x] 缺失软件已安装
- [x] 服务器准备就绪

**完成日期**: 2025-01-22

**注意事项**:
- ⚠️ 端口 80 已被其他网站占用，需要使用其他端口或停止占用服务
- ✅ Docker Compose 使用插件方式：`docker compose` (不是 `docker-compose`)
- ✅ 部署目录已创建：/www/wwwroot/exammaster.zzzjl.com

---

## 任务 15: SSL 证书配置

### 证书信息
- 证书类型: [ ] Let's Encrypt [ ] Cloudflare [ ] 其他
- 证书路径: ___________

### 完成状态
- [ ] 证书已获取
- [ ] 证书已上传到服务器
- [ ] Nginx 已配置

**完成日期**: ___________

---

## 任务 16: 应用部署

### 部署步骤

1. **克隆代码**
```bash
cd /www/wwwroot
git clone https://github.com/xmwengxing/EduMaster_postgresql.git exammaster.zzzjl.com
cd exammaster.zzzjl.com
```

2. **配置环境变量**
```bash
cp .env.example .env
nano .env
# 配置数据库密码、JWT密钥等
```

3. **执行数据迁移**
```bash
# 上传 SQLite 数据库
scp edumaster.db root@47.104.173.139:/www/wwwroot/exammaster.zzzjl.com/

# 执行迁移
node scripts/migrate.js
```

4. **启动服务**
```bash
docker-compose up -d
```

### 完成状态
- [ ] 代码已克隆
- [ ] 环境变量已配置
- [ ] 数据已迁移
- [ ] Docker 服务已启动
- [ ] 网站可以访问

**完成日期**: ___________

---

## 部署验证

### 访问测试
- [ ] https://exammaster.zzzjl.com 可以访问
- [ ] 登录功能正常
- [ ] 题库功能正常
- [ ] 考试功能正常

### 性能测试
- [ ] 响应速度正常
- [ ] 数据库查询正常
- [ ] 并发访问正常

**验证日期**: ___________

---

## 问题记录

### 遇到的问题

1. **问题描述**: 
   - 解决方案: 
   - 解决日期: 

2. **问题描述**: 
   - 解决方案: 
   - 解决日期: 

---

## 备注

（记录其他重要信息）
