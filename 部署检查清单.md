# 部署检查清单

## 📋 本次修复内容

### 1. 学员端性能优化 ⚡
- ✅ 移除全量题目加载（8.24MB → 按需加载）
- ✅ 实现按题库分片缓存
- ✅ 添加 `loadBankQuestions` 函数
- ✅ 自动加载题目逻辑

### 2. 题库显示修复 🔧
- ✅ 修改 `Home.tsx` 使用 `activeBank.questionCount`
- ✅ 添加 `App.tsx` 自动加载逻辑
- ✅ 修复题目数量显示问题
- ✅ 管理员页面加载所有题目

### 3. 管理员认证修复 🔐
- ✅ 修复所有路由的认证中间件配置
- ✅ 添加认证中间件验证脚本

### 4. 加载状态优化 ✨ (新增)
- ✅ 添加 `isLoadingQuestions` 状态
- ✅ 题库管理页面显示加载动画
- ✅ 优化用户体验，避免空白页面

## 🚀 部署步骤

### 步骤 1：本地测试
```bash
# 1. 构建前端
npm run build

# 2. 启动开发服务器
npm run dev

# 3. 测试功能
# - 以学员身份登录
# - 检查首页题库数量
# - 进入练习页面
# - 验证题目加载
```

### 步骤 2：部署到服务器
```bash
# 使用快速部署脚本
./quick-deploy.bat
```

**部署脚本会执行：**
1. ✅ 构建前端 (`npm run build`)
2. ✅ 创建服务器备份
3. ✅ 同步文件到服务器
   - `dist/` - 前端文件
   - `src/` - 后端模块化代码
   - `server-new.js`, `db.js` - 主入口
   - `utils/` - 工具函数
   - `package.json`, `.env.production` - 配置
4. ✅ 安装依赖（如有更新）
5. ✅ 重启 API 服务
6. ✅ 健康检查

### 步骤 3：验证部署

#### 3.1 清除浏览器缓存
```
Windows: Ctrl + Shift + Delete
Mac: Cmd + Shift + Delete
```
选择：
- [x] 缓存的图片和文件
- [x] Cookie 和其他网站数据
- 时间范围：全部时间

#### 3.2 强制刷新页面
```
Windows: Ctrl + Shift + R
Mac: Cmd + Shift + R
```

#### 3.3 测试学员端功能
- [ ] 登录学员账号
- [ ] 首页显示题库数量（不是 0）
- [ ] 点击练习，能看到题目列表
- [ ] 章节筛选正常工作
- [ ] 题型统计正确显示
- [ ] 开始练习能正常答题

#### 3.4 测试管理员功能
- [ ] 登录管理员账号
- [ ] 系统设置能正常保存
- [ ] 标签管理正常
- [ ] 讨论管理正常
- [ ] 实操管理正常

#### 3.5 检查控制台日志
打开浏览器开发者工具（F12），查看 Console 标签：

**应该看到：**
```
[refreshAll] 阶段1: 加载核心数据...
[refreshAll] 使用缓存数据，立即显示页面
[refreshAll] 阶段1完成，页面已可用
[refreshAll] 阶段2: 后台加载次要数据...
[refreshAll] 阶段2完成
[App] 自动加载题库题目: 题库名称 bank-xxx
[loadBankQuestions] 加载题库: bank-xxx
[loadBankQuestions] 加载成功: bank-xxx (123 题)
```

**不应该看到：**
```
❌ ERR_HTTP2_PROTOCOL_ERROR
❌ 401 Unauthorized
❌ [Cache] 数据过大，不缓存
```

## 🐛 故障排查

### 问题 1：题库仍然显示 0 题

**可能原因：**
1. 浏览器缓存未清除
2. 数据库题目数量未更新

**解决方案：**
```bash
# 1. 清除浏览器缓存（见上文）

# 2. 在服务器上更新题库数量
ssh root@47.104.173.139
docker exec edumaster_postgres psql -U edumaster_user -d edumaster -c "
UPDATE banks b
SET question_count = (
  SELECT COUNT(*) FROM questions q WHERE q.bank_id = b.id
);
"

# 3. 重启 API 服务
cd /www/wwwroot/exammaster.zzzjl.com
docker compose restart api
```

### 问题 2：练习页面没有题目

**可能原因：**
1. 题目未自动加载
2. 网络请求失败

**解决方案：**
```javascript
// 在浏览器控制台执行
// 1. 检查 store 状态
console.log('Active Bank:', store.activeBank);
console.log('Questions:', store.questions.length);

// 2. 手动加载题目
await store.loadBankQuestions(store.activeBank.id);

// 3. 检查网络请求
// 打开 Network 标签，查看 /api/questions?bankId=xxx 请求
```

### 问题 3：管理员功能 401 错误

**可能原因：**
1. 认证中间件配置错误
2. Token 过期

**解决方案：**
```bash
# 1. 验证认证中间件配置
node scripts/check-auth-middleware.js

# 2. 重新登录
# 退出登录，清除缓存，重新登录
```

### 问题 4：部署失败

**可能原因：**
1. SSH 连接失败
2. 服务器磁盘空间不足
3. Docker 服务异常

**解决方案：**
```bash
# 1. 检查 SSH 连接
ssh root@47.104.173.139 "echo 'SSH OK'"

# 2. 检查磁盘空间
ssh root@47.104.173.139 "df -h"

# 3. 检查 Docker 状态
ssh root@47.104.173.139 "docker ps"

# 4. 查看 API 日志
ssh root@47.104.173.139 "cd /www/wwwroot/exammaster.zzzjl.com && docker compose logs api --tail=50"
```

## 📊 性能对比

### 优化前
- 首页加载时间：5-10秒
- 数据传输：8.24MB
- 缓存成功率：0%
- 题库显示：❌ 0 题

### 优化后（预期）
- 首页加载时间：<2秒
- 数据传输：<500KB
- 缓存成功率：100%
- 题库显示：✅ 正确数量

## ✅ 完成标志

部署成功的标志：
- ✅ 学员端首页显示正确的题目数量
- ✅ 练习页面能看到题目列表
- ✅ 章节和题型筛选正常
- ✅ 管理员功能正常
- ✅ 控制台无错误
- ✅ 页面加载速度明显提升

## 🔄 回滚方案

如果部署后出现严重问题，可以快速回滚：

```bash
# 在服务器上执行
cd /www/wwwroot/exammaster.zzzjl.com

# 查看备份列表
ls -lt backups/

# 回滚到最新备份
LATEST_BACKUP=$(ls -t backups/ | head -1)
echo "回滚到: $LATEST_BACKUP"

# 恢复文件
cp -r backups/$LATEST_BACKUP/* ./

# 重启服务
docker compose restart api

# 验证
curl http://localhost:3001/api/health
```

## 📞 联系支持

如果遇到无法解决的问题：
1. 查看服务器日志
2. 检查数据库状态
3. 验证网络连接
4. 回滚到上一个版本

---

**创建时间：** 2026-01-27  
**部署人员：** ___________  
**部署时间：** ___________  
**验证结果：** ___________
